# 📌 **Bootstrap Columns for Layout**

Bootstrap provides a **flexible grid system** that helps create responsive layouts. It works based on **columns**, making it easy to adjust the structure for different screen sizes like **desktop, tablet, and mobile**.

---

## ✅ **1. Bootstrap Grid System Overview**

Bootstrap's grid system uses **12 columns** by default. You can divide your layout into **equal or different-sized columns** based on the screen size.

- **`.col-{breakpoint}-{number}`** → Defines column width.
- **Breakpoints:**
  - `col-` → Extra Small (Default, for mobile)
  - `col-sm-` → Small (≥576px, for tablets)
  - `col-md-` → Medium (≥768px, for desktops)
  - `col-lg-` → Large (≥992px)
  - `col-xl-` → Extra Large (≥1200px)
  - `col-xxl-` → Extra Extra Large (≥1400px)

---

## ✅ **2. Basic Column Layout**

Each row is divided into **12 columns**. If you use `col-6`, it means **each column takes up 6/12 (half) of the row**.

### 📌 **Example: Two Equal Columns**

```html copy
<div class="container">
  <div class="row">
    <div class="col-6 bg-primary text-white p-3">Column 1</div>
    <div class="col-6 bg-secondary text-white p-3">Column 2</div>
  </div>
</div>
```

### 🎯 **Explanation:**

- `col-6` → Each column takes **half (6/12) of the row**.
- `bg-primary` & `bg-secondary` → Adds background colors for visibility.

---

## ✅ **3. Responsive Layout for Web, Tablet, and Mobile**

We can define **different column widths** for different screen sizes.

### 📌 **Example: Responsive Columns (Web, Tablet, Mobile)**

```html copy
<div class="container">
  <div class="row">
    <div class="col-md-4 col-sm-6 col-12 bg-success text-white p-3">
      Column 1
    </div>
    <div class="col-md-4 col-sm-6 col-12 bg-danger text-white p-3">
      Column 2
    </div>
    <div class="col-md-4 col-sm-12 col-12 bg-warning text-dark p-3">
      Column 3
    </div>
  </div>
</div>
```

### 🎯 **Explanation:**

- **Web (`col-md-4`)** → 3 columns, each taking **4/12 (one-third) of the row**.
- **Tablet (`col-sm-6`)** → 2 columns per row.
- **Mobile (`col-12`)** → Each column takes **full width** in mobile view.

---

## ✅ **4. Full Layout Example with Web, Tablet, and Mobile Views**

### 📌 **Example: Full Page Layout**

```html copy
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Bootstrap Responsive Columns</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container">
      <h2>Responsive Layout</h2>

      <div class="row">
        <div
          class="col-lg-3 col-md-4 col-sm-6 col-12 bg-primary text-white p-3"
        >
          Box 1
        </div>
        <div
          class="col-lg-3 col-md-4 col-sm-6 col-12 bg-secondary text-white p-3"
        >
          Box 2
        </div>
        <div
          class="col-lg-3 col-md-4 col-sm-6 col-12 bg-success text-white p-3"
        >
          Box 3
        </div>
        <div class="col-lg-3 col-md-4 col-sm-6 col-12 bg-danger text-white p-3">
          Box 4
        </div>
      </div>
    </div>
  </body>
</html>
```

### 🎯 **How It Works on Different Screens:**

✅ **Web (`col-lg-3`)** → 4 equal columns.  
✅ **Tablet (`col-md-4`)** → 3 columns per row.  
✅ **Mobile (`col-sm-6` & `col-12`)** → 2 columns per row, then full width.

---

## ✅ **5. Column Ordering (Rearranging for Different Devices)**

You can **change the order of columns** on different screen sizes using `order-{number}`.

### 📌 **Example: Changing Order for Mobile**

```html copy
<div class="container">
  <div class="row">
    <div class="col-md-4 order-md-1 order-2 bg-primary text-white p-3">
      First on Web, Last on Mobile
    </div>
    <div class="col-md-4 order-md-2 order-1 bg-secondary text-white p-3">
      Second on Web, First on Mobile
    </div>
    <div class="col-md-4 bg-success text-white p-3">Third (Unchanged)</div>
  </div>
</div>
```

### 🎯 **Explanation:**

- `order-md-1 order-2` → First on **web**, last on **mobile**.
- `order-md-2 order-1` → Second on **web**, first on **mobile**.

---

## ✅ **6. Nested Columns (Columns inside Columns)**

You can place **rows inside columns** to create **nested structures**.

### 📌 **Example: Nested Columns**

```html copy
<div class="container">
  <div class="row">
    <div class="col-md-6 bg-primary text-white p-3">
      Parent Column
      <div class="row">
        <div class="col-6 bg-light text-dark p-2">Nested 1</div>
        <div class="col-6 bg-dark text-white p-2">Nested 2</div>
      </div>
    </div>
    <div class="col-md-6 bg-success text-white p-3">Parent Column 2</div>
  </div>
</div>
```

### 🎯 **Explanation:**

- **Outer `row`** → 2 main columns.
- **Inner `row`** → Inside the first column, split into **two equal parts**.

---

## ✅ **7. Equal Height Columns (Using Flexbox)**

Use `align-items-stretch` to **make columns the same height**.

### 📌 **Example: Equal Height Columns**

```html copy
<div class="container">
  <div class="row align-items-stretch">
    <div class="col-md-4 bg-primary text-white p-3">Short Content</div>
    <div class="col-md-4 bg-secondary text-white p-3">
      Tall Content<br />More Text Here
    </div>
    <div class="col-md-4 bg-success text-white p-3">Short Content</div>
  </div>
</div>
```

### 🎯 **Explanation:**

- `align-items-stretch` → Ensures **all columns** have the same height.

---

## 🎯 **Final Thoughts**

✅ Bootstrap columns allow **easy, responsive layout creation**.  
✅ Use `col-{size}-{number}` for **different screen sizes**.  
✅ Use **ordering, nesting, and equal heights** for better layouts.

Now you can **build responsive layouts** like a pro! 🚀🔥
